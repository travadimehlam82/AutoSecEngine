# AutoSecEngine
A behavior-based endpoint security system is a real-time protection mechanism that monitors and analyzes the behavior of processes, file operations, and system activities on an endpoint. Instead of relying on static signatures, this project focuses on dynamic indicators of compromise (loCs).
